# Task Service

HTTP API на Go для управления задачами. В рамках тестового задания сервис расширен поддержкой повторяющихся задач.

## Что реализовано

- сохранение настроек повторяемости прямо в задаче;
- поддержка четырёх сценариев расписания:
  - каждые `N` дней;
  - каждый месяц в конкретный день (`1..30`);
  - по списку конкретных дат;
  - по чётным или нечётным дням месяца;
- автосоздание экземпляров повторяющихся задач без внешнего scheduler/cron;
- обновлённый OpenAPI-контракт;
- тесты на расчёт расписания и материализацию задач.

## Модель решения

Повторяющаяся задача хранится как шаблон (`kind = recurring_template`).

Когда API получает `POST /tasks`, `PUT /tasks/{id}`, `GET /tasks`, `GET /tasks/{id}`, сервис:

1. находит все шаблоны повторяющихся задач;
2. рассчитывает, какие экземпляры должны существовать в ближайшем окне;
3. дозаписывает недостающие задачи как обычные экземпляры (`kind = recurring_occurrence`).

Разовая задача хранится как `kind = one_time`.

Сгенерированный экземпляр связан с шаблоном через `template_id`.

## Поддержанные поля API

Новые поля задачи:

- `kind`
- `template_id`
- `scheduled_at`
- `recurrence`

Структура `recurrence`:

```json
{
  "type": "every_n_days | monthly_by_day | specific_dates | day_parity",
  "every_n_days": 2,
  "day_of_month": 15,
  "specific_dates": ["2026-04-15", "2026-04-20"],
  "day_parity": "even"
}
```

## Примеры запросов

Разовая задача:

```json
{
  "title": "Позвонить пациенту",
  "description": "Уточнить самочувствие после выписки",
  "status": "new",
  "scheduled_at": "2026-04-13T09:00:00Z"
}
```

Повторяющаяся задача каждые 2 дня:

```json
{
  "title": "Ежедневный обзвон",
  "description": "Проверить состояние пациентов",
  "status": "new",
  "scheduled_at": "2026-04-13T09:00:00Z",
  "recurrence": {
    "type": "every_n_days",
    "every_n_days": 2
  }
}
```

Повторяющаяся задача по конкретным датам:

```json
{
  "title": "Сформировать отчёт",
  "description": "Подготовить отчёт для главного врача",
  "status": "new",
  "scheduled_at": "2026-04-13T08:00:00Z",
  "recurrence": {
    "type": "specific_dates",
    "specific_dates": ["2026-04-15", "2026-04-20", "2026-05-01"]
  }
}
```

## Принятые допущения

Эти решения я принял осознанно, потому что в исходном проекте нет отдельного модуля планировщика:

- сервис материализует задачи в скользящем окне на 30 дней вперёд;
- пропущенные задачи за далёкое прошлое не создаются автоматически;
- расчёт расписания выполняется в UTC;
- `scheduled_at` является опорной датой и временем для шаблона;
- `GET /tasks` возвращает и шаблоны, и сгенерированные экземпляры;
- удаление шаблона удаляет и связанные экземпляры;
- при обновлении шаблона будущие экземпляры пересоздаются по новым правилам;
- сгенерированному экземпляру нельзя назначить собственное расписание через `PUT`.

## Запуск

Требования:

- Go `1.23+`
- Docker и Docker Compose

Локально через Docker Compose:

```bash
docker compose up --build
```

Сервис будет доступен по адресу `http://localhost:8080`.

Если Postgres уже поднимался со старой схемой, нужно пересоздать volume:

```bash
docker compose down -v
docker compose up --build
```

Причина: SQL из `migrations/0001_create_tasks.up.sql` монтируется в `docker-entrypoint-initdb.d` и применяется только при инициализации пустого volume.

## Swagger

- UI: `http://localhost:8080/swagger/`
- OpenAPI JSON: `http://localhost:8080/swagger/openapi.json`

## Тесты

Основные тесты добавлены в:

- `internal/domain/task/recurrence_test.go`
- `internal/usecase/task/service_test.go`

Запуск:

```bash
go test ./...
```
